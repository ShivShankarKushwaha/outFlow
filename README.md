[Web Link](https://outflow-client.vercel.app)

i have disabled linkedin scraper route on production.
if you want then clone the repo locally
create a .env file with required credentials (required credentials can be seen in src/env/index.ts file)
install the packages
and run the server as well as the client code
